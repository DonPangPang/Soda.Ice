@foreach (var item in Items)
{
    <SearchListItem Item="@item" Search="@Search"></SearchListItem>
}

@code {
    [Parameter]
    public string Search { get; set; } = "";

    [Parameter]
    public List<VBlogTiny> Items { get; set; } = new();
}
